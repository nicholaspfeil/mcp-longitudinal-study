# Pinned deliberately. This project runs for 12+ months and its measurements
# must stay comparable across that whole window. Do not upgrade casually —
# an upgrade that changes detector behaviour changes the data, not just the code.

requests==2.32.3
# Imported directly by scan/fetch_registry.py for its Retry policy, so it is
# pinned explicitly rather than left to float as a requests dependency.
urllib3==2.7.0
pandas==2.2.3
